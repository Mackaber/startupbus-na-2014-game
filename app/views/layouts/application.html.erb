<!DOCTYPE html>
<html>
<head>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">

	<title>The Game | Startup Bus 2014</title>
	<%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
	<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	<%= csrf_meta_tags %>

  <link href='http://fonts.googleapis.com/css?family=Lato:400,700,800|Montserrat:400,700' rel='stylesheet' type='text/css'>
</head>

<body>
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <%= link_to "The Game", root_path, class: "navbar-brand" %>
        <p class="navbar-text">StartupBus North America 2014</p>
      </div>

      <ul class="nav navbar-nav navbar-right">
      <% if omniauthable_signed_in? %>
        <li>
          <%= content_tag :p, class: "navbar-text" do %>
            <%= link_to current_omniauthable do %>
              <%= image_tag current_omniauthable.photo_url(height: 25, width: 25), class: "avatar" %>
              <%= current_omniauthable.name %>
            <% end %>
          <% end %>
        </li>
        <% if can?(:manage, :all) %>
          <li>
            <%= link_to "Admin", admin_root_path %>
          </li>
        <% end %>
        <li>
          <%= link_to "Sign Out", destroy_omniauthable_session_path, :method => :delete %>
        </li>
      <% else %>
        <%= link_to omniauthable_omniauth_authorize_path(:facebook), class:"btn btn-default btn-facebook" do %>
          <i class="fa fa-facebook"></i> Sign In
        <% end %>
      <% end %>
      </ul>
    </div>
  </nav>

  <div class="container">
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>

    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
