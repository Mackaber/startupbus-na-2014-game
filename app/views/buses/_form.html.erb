<%= form_for(@bus, :html => {:class => 'form-horizontal'}) do |f| %>
  <% if @bus.errors.any? %>
    <div class="alert alert-danger">
      <button type="button" class="close" data-dismiss="alert">x</button>
      <h4><%= pluralize(@bus.errors.count, "error") %> prohibited this team from being saved:</h4>

      <ul>
      <% @bus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label "Bus Name", :class => 'col-sm-3 control-label' %>

    <div class="col-sm-9">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
  </div>

  <%= f.submit "Create Bus", :class => 'btn btn-block btn-primary'  %>
<% end %>