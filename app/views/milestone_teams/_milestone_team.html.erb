<tr class="success">
	<td><%= milestone_team.milestone.name %></td>
	<td><%= milestone_team.milestone.max_points %></td>
	<td><%= milestone_team.milestone.target_completion_date.strftime("%d-%m-%y") %></td>
	<td><%= milestone_team.created_at.strftime("%d-%m-%y") %></td>
</tr>