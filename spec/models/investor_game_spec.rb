require 'spec_helper'

describe InvestorGame do
  let(:game) { InvestorGame.new }
  let(:team) { InvestorGame.new }
  let(:investor) { Investor.new }
  let(:northeast_bus) { Bus.new }
  let(:westcoast_bus) { Bus.new }

  it "should assign a score to each team based on money invested and milestones completed" do 
    
  end

  it "should reflect the number of people in the game to the investor" do 
    
  end

  it "should reflect the time remaining to the investor" do 
    
  end

end
